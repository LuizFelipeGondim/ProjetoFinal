#include "GameBoard.hpp"
//#include "Player.hpp"
#include <iostream>

#include <string>

GameBoard::~GameBoard(){
    //for(auto& player : _players)
        //delete player;
}

void GameBoard::registerPlayer(std::string name, std::string nickname){
    //_players.push_back(new Player(name, nickname));
}

void GameBoard::listStatistics(char orderType){

    
}

bool searchPlayer(std::string nickname){
    if(/*_players.empty()*/1)
        return false;
}
